一社、お見送りとなった。

辛いけど、落ち込まず、前に進もう。

---

オブジェクトとインスタンスについて。

オブジェクトとインスタンスは同じものと説明される。

それも正しい。

でも、オブジェクトの方が広意義に扱われる。

クラスも、インスタンスもオブジェクトと言える。

一般的、設計レベルでは、オブジェクトと言い、クラスから生成されてメモリ上に実態として存在するのはインスタンス。

なので、インスタンスは、オブジェクトの一つに含まれる。

---